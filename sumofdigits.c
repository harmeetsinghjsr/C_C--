#include<stdio.h>
#include<conio.h>
int sumofdigits(int);
int main()
{
	int n,result,t,d;
	printf("Enter a no.");
	scanf("%d",&n);
	result=fact(n);
	printf("%d",n);
	getch();
}
int sumofdigits(int x);
{
	int d,t;
	t=x;
	if(x==0)
	return 0;
	else
	return 
}
